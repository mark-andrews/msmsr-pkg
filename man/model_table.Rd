% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_evaluation.R
\name{model_table}
\alias{model_table}
\title{Model comparison table with AIC weights}
\usage{
model_table(...)
}
\arguments{
\item{...}{Named model objects, or a single named list of models.}
}
\value{
A tibble sorted by AIC with columns: model, k, logLik, AIC, BIC,
delta_AIC, weight.
}
\description{
Creates a comparison table for multiple models showing log-likelihood,
AIC, BIC, delta AIC, and Akaike weights.
}
\examples{
m1 <- lm(mpg ~ wt, mtcars)
m2 <- lm(mpg ~ wt + hp, mtcars)
model_table(simple = m1, full = m2)

# Or pass a list
models <- list(simple = m1, full = m2)
model_table(models)
}
